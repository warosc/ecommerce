{
  "name": "@optimus/contracts",
  "version": "0.1.0",
  "private": true,
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "tsc -p tsconfig.json --noEmit",
    "test": "echo \"(contracts) sin tests: paquete solo de tipos\""
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
